# TanStack Query Rules

All API data fetching must use TanStack Query. When writing or modifying any API call or server state logic, invoke the `tanstack-query` skill via the `Skill` tool before writing code.

For best practices and advanced patterns, refer to the `tanstack-query-best-practices` skill via the `Skill` tool.
